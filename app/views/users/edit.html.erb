<h1>Editing user</h1>

<%= form_for(@user, as: "user", url: user_path(@user), method: :put, :autocomplete => 'off') do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
<% end %>

<%= link_to 'Show', user_path(@user) %> |
<%= link_to 'Back', users_path %>
