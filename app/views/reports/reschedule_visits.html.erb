<div class="row-fluid">
  <div class="span8">
    <div class="row-fluid">
      <div class="span2"><strong>Today's Date</strong></div>
      <div class="span3"><%= Date.today.strftime('%A - %B %d, %Y') %> </div>
    </div>


    <div class="row-fluid marT10">
      <%= search_form_for @search, url: reschedule_visits_reports_path, html: {class: "form-inline"} do |f| %>
          <div class="span5" style="margin-left: 0px !important;">
            <div class="form-group">
              <label class="sr-only" for="exampleInputEmail2">Start Date: </label>
              <%= f.text_field :date_visit_gteq, value: f.object.date_visit_gteq.to_date, class: "form-control dpicker", id: "dpd1" %>
              <i class="icon icon-calendar"></i>
            </div>
          </div>

          <div class="span5">
            <div class="form-group">
              <label class="sr-only" for="exampleInputEmail2">End Date: </label>
              <%= f.text_field :date_visit_lteq, value: f.object.date_visit_lteq.to_date, class: "form-control dpicker", id: "dpd2" %>
              <i class="icon icon-calendar"></i>
            </div>
          </div>

          <div class="span2">
            <%= f.submit "SEARCH", class: "btn" %>
          </div>
      <% end %>
    </div>

  </div>
  <div class="span4"><h2 class="pull-right">Reschedule Visit Report</h2>
  </div>
</div>
<hr>



<div class="row-fluid">
  <div class="span12">

    <br>
    <table class="table table-bordered">
      <thead>
      <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Phone1</th>
        <th>Phone2</th>
        <th>Email</th>
        <th>Date visit</th>
        <th></th>
      </tr>
      </thead>

      <tbody>
      <% @visits.each do |visit| %>
          <tr>
            <td><%= visit.patient.id %></td>
            <td><%= visit.patient.fullname %></td>
            <td><%= visit.patient.phone1 %></td>
            <td><%= visit.patient.phone2 %></td>
            <td><%= visit.patient.email1 %></td>
            <td><%= visit.date_visit.to_date %></td>
            <td><%= link_to 'Show', [visit.patient, visit] %></td>
          </tr>
      <% end %>
      </tbody>
    </table>
    <%= page_entries_info @visits %>
    <%= paginate @visits %>
  </div>
</div>


